#pragma once

// this functions print to std but in a diferente color
// soo the user cona diferentiate wich msg are from his own
// process and wich are form cmon
void cmon_print_msg_to_user(char *msg);
